name: business
description: Application for business.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.1
  formz: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.2
  freezed: ^0.12.7
  equatable: ^1.2.5
  json_serializable: ^3.5.1
flutter:
  uses-material-design: true
